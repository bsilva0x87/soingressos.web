br:
  activerecord:
    models:
      user:
        one: "usuário"
        other: "usuários"
    attributes:
      user:
        name: "nome"
        username: "usuário"
    errors:
      messages:
        record_invalid: '%{errors}'